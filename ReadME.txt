1.) Run the DB Script :script.sql to create the database CMSTweetDB.
2.) Install EF in VS. Run the project by opening CMSJitter .sln file.

[Backend API can be checked with PostMan or Fiddler by sending requests]
[API help is given in help section]


3.) Install angularcli, primgeng and Run the project by CMSJitterUI with "ng serve" and browse to that URL.
Install Entity Framework